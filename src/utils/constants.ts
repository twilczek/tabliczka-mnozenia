/**
 * Application constants
 */

// Delays (in milliseconds)
export const FEEDBACK_DELAY = 1200;  // Delay showing feedback after answering
export const COUNTDOWN_START_DELAY = 50;  // Small delay before starting countdown
export const STORAGE_UPDATE_DELAY = 100;  // Delay to ensure localStorage updates complete 